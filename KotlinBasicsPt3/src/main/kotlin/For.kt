/*
   It provides us with a concise way of writing the loop input.
   A for statement consumes the initialization, condition, and increment / decrement in a
   line, thus providing a shorter loop structure that is easier to debug.
 */

fun main(args: Array<String>) {

    for (i in 1..5) {
        println("Value $i")
    }

}