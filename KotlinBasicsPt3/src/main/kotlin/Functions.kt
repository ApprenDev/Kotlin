/*
  Functions are declared using the fun keyword, followed by the name of the method,
   the parentheses where we will declare the input values, and braces that limit the function.
 */

fun main(args: Array<String>) {
    showSimpleText()
}

fun showSimpleText() {
    println("Android developer course")
}